{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // dsp roda em worker, no navegador e no Node. Nada de DOM.
    "lib": ["ES2023"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
